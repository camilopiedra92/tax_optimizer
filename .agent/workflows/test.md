---
description: Run the Vitest test suite
---

# Run Tests

1. Run the full test suite:
   // turbo

```bash
cd /Users/camilopiedra/Documents/tax_optimizer/tax-optimizer-app && source ./sandbox-env.sh && npm run test
```

2. If tests fail, review the output and fix the failing tests before proceeding.

3. To run tests in watch mode for active development:

```bash
cd /Users/camilopiedra/Documents/tax_optimizer/tax-optimizer-app && source ./sandbox-env.sh && npm run test:watch
```
